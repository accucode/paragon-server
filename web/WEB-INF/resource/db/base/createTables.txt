###############################################################
###############################################################
##
##  AUTO GENERATED - DO NOT EDIT
##
###############################################################
###############################################################


DROP TABLE IF EXISTS applicationLog;
CREATE TABLE applicationLog
(
    uid varchar(30),
    createdUtcTs datetime,
    loggerName varchar(100),
    context varchar(100),
    message varchar(100),
    levelName char(5),
    levelCode int,
    threadName varchar(100),
    trace text,
    primary key (uid)
) ENGINE = INNODB;
create index levelCode on applicationLog (levelCode, uid);
create index createdUtcTs on applicationLog (createdUtcTs, uid);


DROP TABLE IF EXISTS attentionGroup;
CREATE TABLE attentionGroup
(
    uid varchar(30),
    name varchar(50),
    lockVersion int,
    projectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index attentionGroup on attentionGroup (projectUid, name);


DROP TABLE IF EXISTS attributeField;
CREATE TABLE attributeField
(
    uid varchar(30),
    categoryCode char(1),
    name varchar(50),
    active char(1),
    lockVersion int,
    projectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index projectName on attributeField (projectUid, name);


DROP TABLE IF EXISTS attributeValue;
CREATE TABLE attributeValue
(
    uid varchar(30),
    data varchar(100),
    lockVersion int,
    fieldUid varchar(30),
    productUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index field on attributeValue (fieldUid);


DROP TABLE IF EXISTS auditLog;
CREATE TABLE auditLog
(
    uid varchar(30),
    transactionUid varchar(30),
    userName varchar(50),
    typeCode char(1),
    utcTs datetime,
    modelName varchar(50),
    modelUid varchar(30),
    fieldName varchar(50),
    newValue varchar(50),
    oldValue varchar(50),
    stringValue varchar(1000),
    integerValue int,
    longValue bigint,
    doubleValue double,
    moneyValue decimal(20,2),
    booleanValue char(1),
    dateValue date,
    timestampValue datetime,
    uidValue varchar(30),
    userUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index modelField on auditLog (modelUid, fieldName, utcTs);


DROP TABLE IF EXISTS autoSignIn;
CREATE TABLE autoSignIn
(
    uid varchar(30),
    createdUtcTs datetime,
    lastTouchedUtcTs datetime,
    lockVersion int,
    userUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;


DROP TABLE IF EXISTS customer;
CREATE TABLE customer
(
    uid varchar(30),
    name varchar(50),
    discountRate double,
    addressStreet1 varchar(50),
    addressStreet2 varchar(50),
    addressCity varchar(50),
    addressRegion varchar(50),
    addressPostalCode varchar(20),
    addressCountry varchar(50),
    lockVersion int,
    projectUid varchar(30),
    tierUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index project on customer (projectUid);


DROP TABLE IF EXISTS customerContact;
CREATE TABLE customerContact
(
    uid varchar(30),
    name varchar(50),
    title varchar(50),
    phone varchar(30),
    email varchar(50),
    orderNotifications char(1),
    lockVersion int,
    customerUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index customer on customerContact (customerUid);


DROP TABLE IF EXISTS customerSite;
CREATE TABLE customerSite
(
    uid varchar(30),
    name varchar(50),
    addressStreet1 varchar(50),
    addressStreet2 varchar(50),
    addressCity varchar(50),
    addressRegion varchar(50),
    addressPostalCode varchar(20),
    addressCountry varchar(50),
    lockVersion int,
    customerUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index customer on customerSite (customerUid);


DROP TABLE IF EXISTS customerTier;
CREATE TABLE customerTier
(
    uid varchar(30),
    name varchar(50),
    discountRate double,
    lockVersion int,
    projectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index project on customerTier (projectUid);


DROP TABLE IF EXISTS depot;
CREATE TABLE depot
(
    uid varchar(30),
    name varchar(50),
    phone varchar(30),
    lockVersion int,
    projectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index projectName on depot (projectUid, name);


DROP TABLE IF EXISTS download;
CREATE TABLE download
(
    uid varchar(30),
    name varchar(50),
    createdUtcTs datetime,
    lockVersion int,
    userUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index created on download (createdUtcTs);


DROP TABLE IF EXISTS email;
CREATE TABLE email
(
    uid varchar(30),
    createdUtcTs datetime,
    sentUtcTs datetime,
    subject varchar(100),
    fromAddress varchar(50),
    statusCode char(1),
    errorNotes varchar(100),
    lockVersion int,
    primary key (uid)
) ENGINE = INNODB;
create index status on email (statusCode, createdUtcTs);


DROP TABLE IF EXISTS emailPart;
CREATE TABLE emailPart
(
    uid varchar(30),
    sequence int,
    typeCode char(1),
    attachmentName varchar(50),
    data mediumblob,
    lockVersion int,
    emailUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index email on emailPart (emailUid);


DROP TABLE IF EXISTS emailRecipient;
CREATE TABLE emailRecipient
(
    uid varchar(30),
    address varchar(50),
    typeCode char(1),
    lockVersion int,
    emailUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index email on emailRecipient (emailUid);


DROP TABLE IF EXISTS endUser;
CREATE TABLE endUser
(
    uid varchar(30),
    name varchar(50),
    lockVersion int,
    customerUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index customer on endUser (customerUid);


DROP TABLE IF EXISTS endUserSite;
CREATE TABLE endUserSite
(
    uid varchar(30),
    name varchar(50),
    addressStreet1 varchar(50),
    addressStreet2 varchar(50),
    addressCity varchar(50),
    addressRegion varchar(50),
    addressPostalCode varchar(20),
    addressCountry varchar(50),
    lockVersion int,
    endUserUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index endUser on endUserSite (endUserUid);


DROP TABLE IF EXISTS fieldTest;
CREATE TABLE fieldTest
(
    uid varchar(30),
    nameValue varchar(50),
    integerValue int,
    longTest bigint,
    doubleTest double,
    moneyTest decimal(20,2),
    booleanTest char(1),
    dateTest date,
    timestampTest datetime,
    pinNumber1 char(5),
    pinNumber2 char(5),
    lockVersion int,
    userTestUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;


DROP TABLE IF EXISTS file;
CREATE TABLE file
(
    uid varchar(30),
    name varchar(100),
    path varchar(100),
    createdUtcTs datetime,
    statusCode char(1),
    size int,
    partialSize int,
    lockVersion int,
    primary key (uid)
) ENGINE = INNODB;


DROP TABLE IF EXISTS hibernateCacheTest;
CREATE TABLE hibernateCacheTest
(
    uid varchar(30),
    data varchar(1000),
    lockVersion int,
    primary key (uid)
) ENGINE = INNODB;


DROP TABLE IF EXISTS invitation;
CREATE TABLE invitation
(
    uid varchar(30),
    typeCode char(1),
    statusCode char(1),
    createdUtcTs datetime,
    closedUtcTs datetime,
    toEmail varchar(50),
    roleCode char(1),
    lockVersion int,
    fromUserUid varchar(30),
    projectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;


DROP TABLE IF EXISTS masterProduct;
CREATE TABLE masterProduct
(
    uid varchar(30),
    partNumber varchar(50),
    active char(1),
    lockVersion int,
    projectUid varchar(30),
    publishedVersionUid varchar(30),
    draftVersionUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create unique index projectPart on masterProduct (projectUid, partNumber);


DROP TABLE IF EXISTS member;
CREATE TABLE member
(
    uid varchar(30),
    roleCode char(1),
    lockVersion int,
    projectUid varchar(30),
    userUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create unique index uid on member (uid);


DROP TABLE IF EXISTS memberSkill;
CREATE TABLE memberSkill
(
    uid varchar(30),
    sequence int,
    lockVersion int,
    memberUid varchar(30),
    skillUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create unique index memberSkill on memberSkill (memberUid, skillUid);


DROP TABLE IF EXISTS orderNumber;
CREATE TABLE orderNumber
(
    uid varchar(30),
    date date,
    nextNumber int,
    lockVersion int,
    projectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create unique index projectDate on orderNumber (projectUid, date);


DROP TABLE IF EXISTS passwordReset;
CREATE TABLE passwordReset
(
    uid varchar(30),
    email varchar(50),
    token varchar(30),
    createdUtcTs datetime,
    expirationUtcTs datetime,
    lockVersion int,
    primary key (uid)
) ENGINE = INNODB;
create index token on passwordReset (token);
create index expiration on passwordReset (expirationUtcTs);


DROP TABLE IF EXISTS patch;
CREATE TABLE patch
(
    name varchar(50),
    installedUtcTs datetime,
    source text,
    primary key (name)
) ENGINE = INNODB;


DROP TABLE IF EXISTS performanceLogDetail;
CREATE TABLE performanceLogDetail
(
    uid varchar(30),
    name varchar(100),
    createdUtcTs datetime,
    durationMs int,
    primary key (uid)
) ENGINE = INNODB;
create index name on performanceLogDetail (name);
create index createdUtcTs on performanceLogDetail (createdUtcTs, name);


DROP TABLE IF EXISTS performanceLogSummary;
CREATE TABLE performanceLogSummary
(
    uid varchar(30),
    utcDate date,
    name varchar(100),
    count int,
    minimumMs int,
    maximumMs int,
    averageMs int,
    totalMs int,
    primary key (uid)
) ENGINE = INNODB;
create unique index date on performanceLogSummary (utcDate, name);
create index name on performanceLogSummary (name, utcDate);


DROP TABLE IF EXISTS powerType;
CREATE TABLE powerType
(
    uid varchar(30),
    name varchar(50),
    lockVersion int,
    projectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index projectName on powerType (projectUid, name);


DROP TABLE IF EXISTS product;
CREATE TABLE product
(
    uid varchar(30),
    statusCode char(1),
    name varchar(50),
    description varchar(1000),
    listPrice decimal(20,2),
    discountable char(1),
    taxable char(1),
    cost decimal(20,2),
    requiresShip char(1),
    shipInstruction varchar(1000),
    pickInstruction varchar(1000),
    networkPortsProduced int,
    networkPortsConsumed int,
    poePortsProduced int,
    poePortsConsumed int,
    vendorPartNumber int,
    lockVersion int,
    projectUid varchar(30),
    masterUid varchar(30),
    categoryUid varchar(30),
    powerTypeUid varchar(30),
    vendorUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index projectName on product (projectUid, name);


DROP TABLE IF EXISTS productCategory;
CREATE TABLE productCategory
(
    uid varchar(30),
    name varchar(50),
    lockVersion int,
    projectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index projectName on productCategory (projectUid, name);


DROP TABLE IF EXISTS project;
CREATE TABLE project
(
    uid varchar(30),
    name varchar(50),
    orderNumberPrefix char(5),
    lockVersion int,
    primary key (uid)
) ENGINE = INNODB;
create unique index projectName on project (name);
create unique index orderNumber on project (orderNumberPrefix);


DROP TABLE IF EXISTS region;
CREATE TABLE region
(
    uid varchar(30),
    name varchar(50),
    lockVersion int,
    projectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index region on region (projectUid, name);


DROP TABLE IF EXISTS salesOrder;
CREATE TABLE salesOrder
(
    uid varchar(30),
    number varchar(50),
    statusCode char(1),
    holdUntilUtcTs datetime,
    expedite char(1),
    taxExempt char(1),
    taxRate double,
    discountRate double,
    totalPrice decimal(20,2),
    totalTax decimal(20,2),
    lockVersion int,
    projectUid varchar(30),
    customerUid varchar(30),
    regionUid varchar(30),
    attentionToUid varchar(30),
    powerTypeUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index number on salesOrder (number);
create index status on salesOrder (statusCode);


DROP TABLE IF EXISTS salesOrderContact;
CREATE TABLE salesOrderContact
(
    uid varchar(30),
    name varchar(50),
    title varchar(50),
    phone varchar(30),
    email varchar(50),
    orderNotifications char(1),
    lockVersion int,
    salesOrderUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index salesOrder on salesOrderContact (salesOrderUid);


DROP TABLE IF EXISTS salesOrderLine;
CREATE TABLE salesOrderLine
(
    uid varchar(30),
    listPrice decimal(20,2),
    unitPrice decimal(20,2),
    orderedQuantity int,
    fulfilledQuantity int,
    extendedPrice decimal(20,2),
    priceAdjustment decimal(20,2),
    adjustedPrice decimal(20,2),
    tax decimal(20,2),
    totalPrice decimal(20,2),
    lockVersion int,
    salesOrderUid varchar(30),
    productUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index salesOrder on salesOrderLine (salesOrderUid);


DROP TABLE IF EXISTS serverSession;
CREATE TABLE serverSession
(
    uid varchar(30),
    active char(1),
    createdUtcTs datetime,
    closedUtcTs datetime,
    lastTouchedUtcTs datetime,
    version varchar(50),
    lockVersion int,
    userUid varchar(30),
    autoSignInUid varchar(30),
    currentProjectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;


DROP TABLE IF EXISTS settings;
CREATE TABLE settings
(
    code int,
    someMessage varchar(100),
    lockVersion int,
    primary key (code)
) ENGINE = INNODB;


DROP TABLE IF EXISTS shipAccount;
CREATE TABLE shipAccount
(
    uid varchar(30),
    name varchar(50),
    description varchar(1000),
    billedToCustomer char(1),
    shipOnAccountName varchar(50),
    shipOnAccountNumber varchar(50),
    billToTypeCode char(1),
    billToAccount varchar(50),
    lockVersion int,
    projectUid varchar(30),
    customerUid varchar(30),
    carrierUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index projectName on shipAccount (projectUid, name);


DROP TABLE IF EXISTS shipCarrier;
CREATE TABLE shipCarrier
(
    uid varchar(30),
    name varchar(50),
    lockVersion int,
    projectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index projectName on shipCarrier (projectUid, name);


DROP TABLE IF EXISTS shipMethod;
CREATE TABLE shipMethod
(
    uid varchar(30),
    name varchar(50),
    lockVersion int,
    carrierUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index carrier on shipMethod (carrierUid, name);


DROP TABLE IF EXISTS shipment;
CREATE TABLE shipment
(
    uid varchar(30),
    trackingNumber varchar(50),
    weight double,
    cost decimal(20,2),
    invoiceCustomer char(1),
    lockVersion int,
    salesOrderUid varchar(30),
    accountUid varchar(30),
    methodUid varchar(30),
    depotUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index salesOrder on shipment (salesOrderUid);
create index trackingNumber on shipment (trackingNumber);


DROP TABLE IF EXISTS skill;
CREATE TABLE skill
(
    uid varchar(30),
    name varchar(50),
    lockVersion int,
    projectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index projectName on skill (projectUid, name);


DROP TABLE IF EXISTS threadTopic;
CREATE TABLE threadTopic
(
    code varchar(50),
    ownerUid varchar(30),
    hostName varchar(50),
    hostAddress varchar(50),
    lastStartUtcTs datetime,
    lastEndUtcTs datetime,
    lastTouchUtcTs datetime,
    lockVersion int,
    primary key (code)
) ENGINE = INNODB;


DROP TABLE IF EXISTS user;
CREATE TABLE user
(
    uid varchar(30),
    name varchar(50),
    email varchar(50),
    phone varchar(30),
    verified char(1),
    passwordSalt varchar(30),
    passwordHash varchar(40),
    timeZoneCode char(5),
    roleCode char(1),
    lockVersion int,
    lastProjectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create unique index email on user (email);


DROP TABLE IF EXISTS userActivation;
CREATE TABLE userActivation
(
    uid varchar(30),
    email varchar(50),
    token varchar(30),
    createdUtcTs datetime,
    expirationUtcTs datetime,
    lockVersion int,
    primary key (uid)
) ENGINE = INNODB;
create index token on userActivation (token);
create index expiration on userActivation (expirationUtcTs);


DROP TABLE IF EXISTS vendor;
CREATE TABLE vendor
(
    uid varchar(30),
    name varchar(50),
    lockVersion int,
    projectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index projectName on vendor (projectUid, name);


DROP TABLE IF EXISTS visitType;
CREATE TABLE visitType
(
    uid varchar(30),
    name varchar(50),
    lockVersion int,
    projectUid varchar(30),
    primary key (uid)
) ENGINE = INNODB;
create index projectName on visitType (projectUid, name);


